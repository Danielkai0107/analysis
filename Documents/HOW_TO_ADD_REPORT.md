# 📝 如何新增報告

## 🎯 簡介

本系統採用**數據驅動**的架構設計，新增報告時**只需添加數據配置**，無需創建新組件！

## ✨ 架構說明

### 核心組件

1. **`ReportViewer.tsx`** - 通用報告查看器組件
   - 唯一的報告顯示組件
   - 根據傳入的數據自動渲染所有內容

2. **`reportsData.ts`** - 報告數據配置文件
   - 包含所有報告的數據
   - 新增報告只需在此添加數據

3. **`report.ts`** - TypeScript 類型定義
   - 定義報告數據結構
   - 確保類型安全

## 🚀 新增報告的步驟

### 步驟 1: 準備數據

從 TXT 報告文件中提取以下數據：

```typescript
{
  id: '唯一識別碼',
  title: '報告標題',
  icon: '圖標 emoji',
  reportTime: '報告生成時間',
  overview: { 總覽數據 },
  onlineVsOffline: [ 線上線下對比 ],
  categoryData: [ 分類數據 ],
  topTags: [ 標籤數據 ],
  tagCombinations: [ 標籤組合 ],
  weekdayData: [ 星期數據 ],
  monthlyData: [ 月份數據 ],
  weekdayVsWeekend: [ 平假日數據 ],
  mostPopularDay: { 最熱門日期 },
  topEvents: [ 高互動率活動 ],
  titleKeywords: [ 標題關鍵字 ],
  market: { 市場機會數據 }
}
```

### 步驟 2: 添加到配置文件

打開 `app/data/reportsData.ts`，在 `reportsData` 陣列中添加新報告數據：

```typescript
export const reportsData: ReportData[] = [
  // 現有報告...
  
  // 新增報告
  {
    id: 'new-report',  // 唯一 ID
    title: '新報告類活動數據分析報告',
    icon: '🎯',
    reportTime: '2025-11-10 16:00:00',
    
    overview: {
      total: 50,
      dateRange: '2025-11-01 至 2026-01-01',
      avgInterval: 2.5,
      avgMonthly: 12.0,
      days: 61
    },
    
    // ... 其他數據
  }
];
```

### 步驟 3: 完成！

就這樣！系統會自動：
- ✅ 在報告選擇器中顯示新報告按鈕
- ✅ 自動渲染所有 6 個分頁的內容
- ✅ 自動生成所有圖表和表格
- ✅ 更新報告總數統計

## 📋 數據結構詳解

### 1. 基本資訊
```typescript
id: string;           // 唯一識別碼，用於切換報告
title: string;        // 報告標題
icon: string;         // emoji 圖標
reportTime: string;   // 報告生成時間
```

### 2. 總覽數據
```typescript
overview: {
  total: number;        // 活動總數
  dateRange: string;    // 資料時間範圍
  avgInterval: number;  // 平均活動間隔（天）
  avgMonthly: number;   // 月均活動數
  days: number;         // 涵蓋天數
}
```

### 3. 線上線下數據
```typescript
onlineVsOffline: [
  { 
    type: '線下活動', 
    count: 45,      // 活動數量
    avgView: 692,   // 平均觀看
    avgLike: 12     // 平均喜歡
  },
  { type: '線上活動', count: 4, avgView: 217, avgLike: 10 }
]
```

### 4. 分類數據
```typescript
categoryData: [
  {
    name: '分類名稱',
    view: 1376,    // 平均觀看數
    like: 51,      // 平均喜歡數
    count: 1       // 活動數量
  }
]
```

### 5. 標籤數據
```typescript
topTags: [
  {
    name: '標籤名稱',
    count: 5,      // 使用次數
    view: 620,     // 平均觀看
    like: 14       // 平均喜歡
  }
]

tagCombinations: [
  '標籤1 + 標籤2 (3場)',
  '標籤3 + 標籤4 (2場)'
]
```

### 6. 時間數據
```typescript
weekdayData: [
  { day: '星期一', count: 1, view: 32, like: 1 }
]

monthlyData: [
  { month: '2025-11', count: 30, percentage: 60 }
]

weekdayVsWeekend: [
  { type: '平日', count: 22, percentage: 44 },
  { type: '假日', count: 28, percentage: 56 }
]

mostPopularDay: {
  day: '星期六',
  count: 17
}
```

### 7. 內容吸引力
```typescript
topEvents: [
  { title: '活動標題', rate: 6.18 }  // 互動率
]

titleKeywords: ['關鍵字1', '關鍵字2']
```

### 8. 市場機會
```typescript
market: {
  redOcean: [            // 紅海市場（競爭激烈）
    { name: 'AI', count: 10 }
  ],
  blueOcean: [           // 藍海市場（機會領域）
    { name: '消費趨勢', count: 2 }
  ],
  learningSubmarket: [   // 細分市場
    { name: 'AI', count: 8 }
  ],
  insights: [            // 市場洞察
    '假日活動佔比 56%，週末是主要活動時段',
    'AI 相關活動競爭最激烈'
  ]
}
```

## 🎨 自訂按鈕顏色

在 `app/reports/page.tsx` 中的 `getButtonColor` 函數添加新顏色：

```typescript
const getButtonColor = (reportId: string) => {
  const colorMap: Record<string, string> = {
    'learning': 'blue',
    'arts': 'purple',
    'experience': 'green',
    'new-report': 'red'  // 新增報告的顏色
  };
  return colorMap[reportId] || 'blue';
};
```

並在按鈕的 `style` 屬性中添加對應的顏色值：

```typescript
style={isSelected ? {
  backgroundColor: color === 'blue' ? '#2563eb' : 
                  color === 'purple' ? '#9333ea' : 
                  color === 'green' ? '#16a34a' :
                  color === 'red' ? '#dc2626' :  // 新增
                  '#2563eb'
} : undefined}
```

## 📊 完整範例

以下是一個完整的報告數據範例：

```typescript
{
  id: 'technology',
  title: '科技類活動數據分析報告',
  icon: '💻',
  reportTime: '2025-11-10 16:30:00',
  
  overview: {
    total: 45,
    dateRange: '2025-11-01 至 2026-01-15',
    avgInterval: 2.0,
    avgMonthly: 14.0,
    days: 75
  },
  
  onlineVsOffline: [
    { type: '線下活動', count: 30, avgView: 850, avgLike: 15 },
    { type: '線上活動', count: 15, avgView: 450, avgLike: 12 }
  ],
  
  categoryData: [
    { name: 'AI', view: 2500, like: 35, count: 10 },
    { name: '區塊鏈', view: 1800, like: 25, count: 8 },
    { name: '雲端運算', view: 1500, like: 20, count: 6 }
  ],
  
  // ... 其他數據
}
```

## ✅ 檢查清單

新增報告前，請確認：

- [ ] 已準備所有必要數據
- [ ] ID 是唯一的（不與現有報告重複）
- [ ] 所有數組至少有一個元素
- [ ] 數字類型正確（不是字串）
- [ ] 日期格式正確
- [ ] 已測試顯示效果

## 🔍 常見問題

### Q: 如果某些數據缺失怎麼辦？

A: 可以提供空數組或預設值：
```typescript
topTags: [],  // 空數組
titleKeywords: ['無']  // 預設值
```

### Q: 可以調整圖表顏色嗎？

A: 可以！在 `ReportViewer.tsx` 中修改 `COLORS` 陣列：
```typescript
const COLORS = ['#3b82f6', '#8b5cf6', '#ec4899', ...];
```

### Q: 如何修改現有報告的數據？

A: 直接在 `reportsData.ts` 中修改對應報告的數據即可，系統會自動更新顯示。

## 🎉 優點

使用此架構的優點：

✅ **簡單** - 只需添加數據，無需寫代碼
✅ **快速** - 幾分鐘內完成新報告
✅ **一致** - 所有報告使用相同的樣式和布局
✅ **可維護** - 數據與視圖分離，易於維護
✅ **可擴展** - 可輕鬆添加無限數量的報告
✅ **型別安全** - TypeScript 確保數據正確性

## 📞 需要幫助？

如有問題，請檢查：
1. TypeScript 類型定義 (`app/types/report.ts`)
2. 現有報告數據範例 (`app/data/reportsData.ts`)
3. 報告查看器組件 (`app/components/ReportViewer.tsx`)

---

**更新日期**: 2025-11-10
**架構版本**: 2.0 (數據驅動架構)

